"use client";

import Navbar from "../Navbar";
import "./header.scss";

const Header = () => {
  return (
    <header className='header'>
      <Navbar />
    </header>
  );
};

export default Header;
